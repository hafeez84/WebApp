@model WebApp.Models.ProductsView

<h3> Last Products </h3>
<div class="col-md-7 ">
    <div class="row">
        <div id="my-pics" class="col-md-12 carousel slide" data-ride="carousel">

            <ol class="carousel-indicators">
                <li data-target="#my-pics" data-slide-to="0" class="active"></li>
                <li data-target="#my-pics" data-slide-to="1"></li>
                <li data-target="#my-pics" data-slide-to="2"></li>
            </ol>

            <div class="carousel-inner" role="listbox">
                @{
                    int num = 1;

                    <div class="item active">
                        <div class="row">
                            <div class="col-md-offset-2">
                                @foreach (var i in Model.Carousel_ps)
                                {
                                    <div class="thumbnail col-md-2" style="width: 260px; height:380px; margin: 2px;">
                                        @foreach (var photo in Model.P_Photos)
                                        {
                                            if (i.Id == photo.P_id)
                                            {
                                                <img src="data:image;base64,@System.Convert.ToBase64String(photo.Photo)" style="width: 255px; height:200px;" />
                                            }
                                        }
                                        @Html.ActionLink("Add to cart", "ToCart", "Cart", new { id = i.Id }, new { @class = "badge " })
                                        <div class="caption">
                                            <div>
                                                <strong>
                                                    @Html.ActionLink(i.Pname, "Details", "Products", new { id = i.Id }, new { @class = "text-capitalize" })
                                                </strong>
                                            </div>
                                            <sub>
                                                @Html.Label("Amount: ") @Html.DisplayFor(modeli => i.Amount)

                                                @foreach (var b in Model.Product_b)
                                                {
                                                    if (b.Id == i.B_id)
                                                    {
                                                        @Html.Label("| Brand: ")
                                                        @Html.DisplayFor(modelItem => b.Name)
                                                    }
                                                }
                                                <br />
                                                @foreach (var m in Model.Product_m)
                                                {
                                                    if (m.Id == i.M_id)
                                                    {
                                                        @Html.Label("Model: ")
                                                        @Html.DisplayFor(modelItem => m.Name)
                                                    }
                                                }
                                            </sub>
                                        </div>
                                        <p class="t-description" style="margin:0; text-overflow:ellipsis;">
                                            @Html.DisplayFor(modeli => i.Pdescription)
                                        </p>
                                        <sub>
                                            Uploaded at: @Html.DisplayFor(modeli => i.Created_at)
                                        </sub>
                                        @{
                                            if (Session["c_id"] != null)
                                            {
                                                var s_id = (int)Session["c_id"];
                                                if (s_id == i.Cid)
                                                {
                                                    <hr />
                                                    <span>
                                                        @Html.ActionLink("Edit", "Edit", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                        @Html.ActionLink("Details", "Details", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                        @Html.ActionLink("Delete", "Delete", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                    </span>
                                                }
                                            }
                                        }
                                    </div>

                                    num += 1;
                                    if (num > 3)
                                    {
                                        num = 0;
                                        break;
                                    }
                                }
                            </div>
                        </div>
                    </div>

                    <div class="item">
                        <div class="row">
                            <div class="col-md-offset-2">

                                @foreach (var i in Model.Carousel_ps)
                                {
                                    if (num > 3 && num <= 6)
                                    {
                                        <div class="thumbnail col-md-2" style="width: 260px; height:380px; margin: 5px;">
                                            @foreach (var photo in Model.P_Photos)
                                            {
                                                if (i.Id == photo.P_id)
                                                {
                                                    <img src="data:image;base64,@System.Convert.ToBase64String(photo.Photo)" style="width: 255px; height:200px;" />
                                                }
                                            }
                                            @Html.ActionLink("Add to cart", "ToCart", "Cart", new { id = i.Id }, new { @class = "badge " })
                                            <div class="caption">
                                                <div>
                                                    <strong>
                                                        @Html.ActionLink(i.Pname, "Details", "Products", new { id = i.Id }, new { @class = "text-capitalize" })
                                                    </strong>
                                                </div>
                                                <sub>
                                                    @Html.Label("Amount: ") @Html.DisplayFor(modeli => i.Amount)

                                                    @foreach (var b in Model.Product_b)
                                                    {
                                                        if (b.Id == i.B_id)
                                                        {
                                                            @Html.Label("| Brand: ")
                                                            @Html.DisplayFor(modelItem => b.Name)
                                                        }
                                                    }
                                                    <br />
                                                    @foreach (var m in Model.Product_m)
                                                    {
                                                        if (m.Id == i.M_id)
                                                        {
                                                            @Html.Label("Model: ")
                                                            @Html.DisplayFor(modelItem => m.Name)
                                                        }
                                                    }
                                                </sub>
                                            </div>
                                            <p class="t-description" style="margin:0; text-overflow:ellipsis;">
                                                @Html.DisplayFor(modeli => i.Pdescription)
                                            </p>
                                            <sub>
                                                Uploaded at: @Html.DisplayFor(modeli => i.Created_at)
                                            </sub>
                                            @{
                                                if (Session["c_id"] != null)
                                                {
                                                    var s_id = (int)Session["c_id"];
                                                    if (s_id == i.Cid)
                                                    {
                                                        <hr />
                                                        <span>
                                                            @Html.ActionLink("Edit", "Edit", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                            @Html.ActionLink("Details", "Details", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                            @Html.ActionLink("Delete", "Delete", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                        </span>
                                                    }
                                                }
                                            }
                                        </div>

                                    }
                                    num += 1;
                                    if (num > 6)
                                    {
                                        num = 0;
                                        break;
                                    }
                                }
                            </div>
                        </div>
                    </div>

                    <div class="item ">
                        <div class="row">
                            <div class="col-md-offset-2">

                                @foreach (var i in Model.Carousel_ps)
                                {
                                    if (num > 6)
                                    {
                                        <div class="thumbnail col-md-2" style="width: 260px; height:380px; margin: 5px;">
                                            @foreach (var photo in Model.P_Photos)
                                            {
                                                if (i.Id == photo.P_id)
                                                {
                                                    <img src="data:image;base64,@System.Convert.ToBase64String(photo.Photo)" style="width: 255px; height:200px;" />
                                                }
                                            }
                                            @Html.ActionLink("Add to cart", "ToCart", "Cart", new { id = i.Id }, new { @class = "badge " })
                                            <div class="caption">
                                                <div>
                                                    <strong>
                                                        @Html.ActionLink(i.Pname, "Details", "Products", new { id = i.Id }, new { @class = "text-capitalize" })
                                                    </strong>
                                                </div>
                                                <sub>
                                                    @Html.Label("Amount: ") @Html.DisplayFor(modeli => i.Amount)

                                                    @foreach (var b in Model.Product_b)
                                                    {
                                                        if (b.Id == i.B_id)
                                                        {
                                                            @Html.Label("| Brand: ")
                                                            @Html.DisplayFor(modelItem => b.Name)
                                                        }
                                                    }
                                                    <br />
                                                    @foreach (var m in Model.Product_m)
                                                    {
                                                        if (m.Id == i.M_id)
                                                        {
                                                            @Html.Label("Model: ")
                                                            @Html.DisplayFor(modelItem => m.Name)
                                                        }
                                                    }
                                                </sub>
                                            </div>
                                            <p class="t-description" style="margin:0; text-overflow:ellipsis;">
                                                @Html.DisplayFor(modeli => i.Pdescription)
                                            </p>
                                            <sub>
                                                Uploaded at: @Html.DisplayFor(modeli => i.Created_at)
                                            </sub>
                                            @{
                                                if (Session["c_id"] != null)
                                                {
                                                    var s_id = (int)Session["c_id"];
                                                    if (s_id == i.Cid)
                                                    {
                                                        <hr />
                                                        <span>
                                                            @Html.ActionLink("Edit", "Edit", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                            @Html.ActionLink("Details", "Details", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                            @Html.ActionLink("Delete", "Delete", new { id = i.Id }, new { @class = "btn btn-info btn-xs" })
                                                        </span>
                                                    }
                                                }
                                            }
                                        </div>

                                    }
                                    num += 1;
                                    if (num >= 9)
                                    {
                                        break;
                                    }
                                }
                            </div>
                        </div>
                    </div>
                }
            </div>

        </div>
        <div class="left carousel-control clearfix" href="#my-pics" role="button" data-slide="prev">
            <span class="icon-prev" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </div>
        <div class="right carousel-control clearfix" href="#my-pics" role="button" data-slide="next">
            <span class="icon-next" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </div>
    </div>
</div>
